<UserControl x:Class="EquipmentDesigner.Controls.CustomContextMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EquipmentDesigner.Controls"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="200"
             Focusable="True">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ContextMenuStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Boolean to Visibility Converter -->
            <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>

            <!-- Inverse Boolean to Visibility Converter -->
            <local:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisConverter"/>

            <!-- Menu Item Type to Foreground Converter -->
            <local:MenuItemTypeToForegroundConverter x:Key="TypeToForegroundConverter"/>

        </ResourceDictionary>
    </UserControl.Resources>

    <!-- This UserControl itself is invisible - the menu appears in a Popup -->
    <Grid x:Name="RootGrid"/>

</UserControl>
