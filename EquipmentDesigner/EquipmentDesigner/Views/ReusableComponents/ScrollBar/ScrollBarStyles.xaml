<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================================================
         Global ScrollBar Styles
         모든 ScrollViewer/ScrollBar에 자동 적용되는 모던 스크롤바 스타일
         ============================================================ -->

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../../Themes/Tokens/Colors.xaml"/>
        <ResourceDictionary Source="../../Themes/Tokens/Spacing.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- =========================
         ScrollBar Semantic Brushes
         ========================= -->
    <SolidColorBrush x:Key="ScrollBar.Track.Background" Color="Transparent"/>
    <SolidColorBrush x:Key="ScrollBar.Thumb.Background" Color="{StaticResource Color.Gray.300}"/>
    <SolidColorBrush x:Key="ScrollBar.Thumb.Background.Hover" Color="{StaticResource Color.Gray.400}"/>
    <SolidColorBrush x:Key="ScrollBar.Thumb.Background.Pressed" Color="{StaticResource Color.Gray.500}"/>

    <!-- =========================
         Thumb Style (Pill Shape)
         ========================= -->
    <Style x:Key="ModernScrollBarThumbStyle" TargetType="{x:Type Thumb}">
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <!-- 
                         Thumb 너비: 6px (ScrollBar 10px - Margin 2*2px)
                         CornerRadius: 3px (너비의 절반) = 완벽한 pill shape
                    -->
                    <Border x:Name="ThumbBorder"
                            CornerRadius="3"
                            Background="{StaticResource ScrollBar.Thumb.Background}"
                            BorderBrush="Transparent"
                            BorderThickness="0"
                            Margin="2"
                            MinHeight="20"/>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ThumbBorder" Property="Background"
                                    Value="{StaticResource ScrollBar.Thumb.Background.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="ThumbBorder" Property="Background"
                                    Value="{StaticResource ScrollBar.Thumb.Background.Pressed}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- =========================
         Global ScrollBar Style (Vertical)
         x:Key 없음 - 모든 ScrollBar에 자동 적용
         ========================= -->
    <Style TargetType="{x:Type ScrollBar}">
        <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
        <Setter Property="Background" Value="{StaticResource ScrollBar.Track.Background}"/>
        <Setter Property="Width" Value="10"/>
        <Setter Property="MinWidth" Value="10"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <Grid x:Name="GridRoot"
                          Width="10"
                          Background="{TemplateBinding Background}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Track x:Name="PART_Track"
                               IsDirectionReversed="True"
                               Focusable="False">
                            <Track.Thumb>
                                <Thumb Style="{StaticResource ModernScrollBarThumbStyle}"/>
                            </Track.Thumb>
                        </Track>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!-- Horizontal Orientation -->
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter Property="Width" Value="Auto"/>
                            <Setter Property="MinWidth" Value="0"/>
                            <Setter Property="Height" Value="10"/>
                            <Setter Property="MinHeight" Value="10"/>
                            <Setter TargetName="GridRoot" Property="Width" Value="Auto"/>
                            <Setter TargetName="GridRoot" Property="Height" Value="10"/>
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>