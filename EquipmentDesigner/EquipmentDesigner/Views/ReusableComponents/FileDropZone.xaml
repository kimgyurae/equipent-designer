<UserControl x:Class="EquipmentDesigner.Controls.FileDropZone"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             lex:LocalizeDictionary.DesignCulture="ko"
             lex:ResxLocalizationProvider.DefaultAssembly="EquipmentDesigner"
             lex:ResxLocalizationProvider.DefaultDictionary="Resources.Strings"
             mc:Ignorable="d"
             x:Name="DropZoneRoot"
             d:DesignHeight="250" d:DesignWidth="400">

    <StackPanel>
        <!-- Label -->
        <TextBlock Text="{Binding LabelText, ElementName=DropZoneRoot}"
                   Style="{StaticResource Typography.Label}"
                   Foreground="{StaticResource Brush.Text.Primary}"
                   Margin="0,0,0,8"/>

        <!-- Drag and Drop Zone -->
        <Border x:Name="DropZoneBorder"
                BorderBrush="{StaticResource Brush.Border.Primary}"
                BorderThickness="1.6"
                CornerRadius="{StaticResource Radius.MD}"
                Background="Transparent"
                MinHeight="160"
                AllowDrop="True"
                Drop="DropZoneBorder_Drop"
                DragEnter="DropZoneBorder_DragEnter"
                DragLeave="DropZoneBorder_DragLeave"
                DragOver="DropZoneBorder_DragOver"
                MouseLeftButtonUp="DropZoneBorder_MouseLeftButtonUp"
                Cursor="Hand">
            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center">
                <!-- Upload Icon -->
                <Path Data="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"
                      Stroke="{StaticResource Brush.Text.Secondary}"
                      StrokeThickness="2"
                      Width="32" Height="32"
                      Stretch="Uniform"
                      HorizontalAlignment="Center"/>

                <!-- Primary Text -->
                <TextBlock Text="{lex:Loc Key=Equipment_DropZone_Primary}"
                           Style="{StaticResource Typography.Body}"
                           Foreground="{StaticResource Brush.Text.Secondary}"
                           HorizontalAlignment="Center"
                           Margin="0,8,0,0"/>

                <!-- Secondary Text -->
                <TextBlock Text="{lex:Loc Key=Equipment_DropZone_Secondary}"
                           Style="{StaticResource Typography.Body}"
                           Foreground="{StaticResource Brush.Text.Secondary}"
                           HorizontalAlignment="Center"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Document List -->
        <ItemsControl ItemsSource="{Binding Documents, ElementName=DropZoneRoot}"
                      Margin="0,12,0,0">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource Brush.Background.Tertiary}"
                            CornerRadius="{StaticResource Radius.MD}"
                            Padding="12,8"
                            Margin="0,0,0,4">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Path Grid.Column="0"
                                  Data="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
                                  Stroke="{StaticResource Brush.Text.Secondary}"
                                  StrokeThickness="1.5"
                                  Width="16" Height="16"
                                  Stretch="Uniform"
                                  VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1"
                                       Text="{Binding}"
                                       Style="{StaticResource Typography.Body.Small}"
                                       Foreground="{StaticResource Brush.Text.Primary}"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0"
                                       TextTrimming="CharacterEllipsis"/>
                            <Button Grid.Column="2"
                                    Click="RemoveDocument_Click"
                                    Tag="{Binding}"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Foreground="{StaticResource Brush.Text.Tertiary}"
                                    FontSize="12"
                                    Cursor="Hand"
                                    Padding="4"
                                    IsEnabled="{Binding IsEditable, ElementName=DropZoneRoot}">
                                <Path Data="M6 18L18 6M6 6l12 12"
                                      Stroke="{StaticResource Brush.Text.Tertiary}"
                                      StrokeThickness="1.5"
                                      Width="10" Height="10"
                                      Stretch="Uniform"/>
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
