<UserControl x:Class="EquipmentDesigner.Views.ProcessDefineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             lex:LocalizeDictionary.DesignCulture="ko"
             lex:ResxLocalizationProvider.DefaultAssembly="EquipmentDesigner"
             lex:ResxLocalizationProvider.DefaultDictionary="Resources.Strings"
             mc:Ignorable="d"
             Background="{StaticResource Brush.Background.Secondary}"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Bar with Back Button -->
        <Border Grid.Row="0"
                Background="{StaticResource Brush.Surface.Primary}"
                BorderBrush="{StaticResource Brush.Border.Primary}"
                BorderThickness="0,0,0,1"
                Padding="24,12"
                Visibility="{Binding ShowBackButton, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Back to Hardware Define Button -->
                <Button Grid.Column="0"
                        Command="{Binding BackToHardwareDefineCommand}"
                        Padding="{StaticResource Spacing.Padding.Button}"
                        Height="{StaticResource Size.Button.Height.MD}"
                        Background="{StaticResource Brush.Button.Secondary.Background}"
                        BorderThickness="0"
                        Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M15 19l-7-7 7-7"
                              Stroke="{StaticResource Brush.Text.Primary}"
                              StrokeThickness="2"
                              Width="16" Height="16"
                              Stretch="Uniform"
                              VerticalAlignment="Center"/>
                        <TextBlock Text="{lex:Loc Key=Process_BackToHardwareDefine}"
                                   Style="{StaticResource Typography.Button}"
                                   Foreground="{StaticResource Brush.Text.Primary}"
                                   Margin="8,0,0,0"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- Title -->
                <TextBlock Grid.Column="1"
                           Text="{lex:Loc Key=Process_Title}"
                           Style="{StaticResource Typography.H4}"
                           Foreground="{StaticResource Brush.Text.Primary}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Main Content Area (Empty for now) -->
        <Grid Grid.Row="1" Margin="{StaticResource Spacing.Padding.2XL}">
            <Border Background="{StaticResource Brush.Surface.Primary}"
                    BorderBrush="{StaticResource Brush.Border.Primary}"
                    BorderThickness="{StaticResource Border.Thin}"
                    CornerRadius="{StaticResource Radius.LG}"
                    Padding="{StaticResource Spacing.Padding.Card}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="{lex:Loc Key=Process_EmptyState}"
                               Style="{StaticResource Typography.H4}"
                               Foreground="{StaticResource Brush.Text.Secondary}"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="{lex:Loc Key=Process_EmptyStateHint}"
                               Style="{StaticResource Typography.Body}"
                               Foreground="{StaticResource Brush.Text.Tertiary}"
                               HorizontalAlignment="Center"
                               Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
