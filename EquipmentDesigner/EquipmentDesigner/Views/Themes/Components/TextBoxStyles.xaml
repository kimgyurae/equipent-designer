<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:reusable="clr-namespace:EquipmentDesigner.Controls">

    <!-- ============================================================
         TextBox Styles with Placeholder Support
         AttachedProperty 기반 Placeholder 지원 TextBox 스타일

         Usage:
         <TextBox reusable:TextBoxHelper.Placeholder="Enter text..."
                  Style="{StaticResource PlaceholderTextBox}"/>
         ============================================================ -->

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Tokens/Colors.xaml"/>
        <ResourceDictionary Source="../Tokens/Brushes.xaml"/>
        <ResourceDictionary Source="../Tokens/Typography.xaml"/>
        <ResourceDictionary Source="../Tokens/Spacing.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- TextBox Style with Placeholder Support -->
    <Style x:Key="PlaceholderTextBox" TargetType="TextBox">
        <Setter Property="Height" Value="{StaticResource Size.Input.Height.MD}"/>
        <Setter Property="Padding" Value="{StaticResource Spacing.Padding.Input}"/>
        <Setter Property="Background" Value="{StaticResource Brush.Input.Background}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Input.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Border.Thin}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
        <Setter Property="FontFamily" Value="{StaticResource Font.Family.Primary}"/>
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Base}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="reusable:TextBoxHelper.IsMonitoring" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource Radius.LG}">
                        <Grid>
                            <!-- Scroll Viewer for text content -->
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="0"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>

                            <!-- Placeholder TextBlock -->
                            <TextBlock x:Name="PlaceholderText"
                                       Text="{TemplateBinding reusable:TextBoxHelper.Placeholder}"
                                       Foreground="{StaticResource Brush.Text.Tertiary}"
                                       FontFamily="{TemplateBinding FontFamily}"
                                       FontSize="{TemplateBinding FontSize}"
                                       Padding="{TemplateBinding Padding}"
                                       VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                       HorizontalAlignment="Left"
                                       IsHitTestVisible="False"
                                       Visibility="Collapsed"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Show placeholder when no text -->
                        <Trigger Property="reusable:TextBoxHelper.HasText" Value="False">
                            <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                        </Trigger>

                        <!-- Hide placeholder when focused (optional - comment out if you want it to stay) -->
                        <!--<Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="PlaceholderText" Property="Visibility" Value="Collapsed"/>
                        </Trigger>-->

                        <!-- Hover State -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource Brush.Input.Background.Hover}"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource Brush.Input.Border.Hover}"/>
                        </Trigger>

                        <!-- Focus State -->
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource Brush.Input.Border.Focus}"/>
                        </Trigger>

                        <!-- Disabled State -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource Brush.Input.Background.Disabled}"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource Brush.Border.Disabled}"/>
                            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Disabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Multiline TextBox Style with Placeholder Support -->
    <Style x:Key="PlaceholderTextBoxMultiline" TargetType="TextBox" BasedOn="{StaticResource PlaceholderTextBox}">
        <Setter Property="Height" Value="Auto"/>
        <Setter Property="MinHeight" Value="80"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="AcceptsReturn" Value="True"/>
        <Setter Property="VerticalContentAlignment" Value="Top"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource Radius.LG}">
                        <Grid>
                            <!-- Scroll Viewer for text content -->
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="0"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}"/>

                            <!-- Placeholder TextBlock -->
                            <TextBlock x:Name="PlaceholderText"
                                       Text="{TemplateBinding reusable:TextBoxHelper.Placeholder}"
                                       Foreground="{StaticResource Brush.Text.Tertiary}"
                                       FontFamily="{TemplateBinding FontFamily}"
                                       FontSize="{TemplateBinding FontSize}"
                                       Padding="{TemplateBinding Padding}"
                                       VerticalAlignment="Top"
                                       HorizontalAlignment="Left"
                                       IsHitTestVisible="False"
                                       Visibility="Collapsed"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Show placeholder when no text -->
                        <Trigger Property="reusable:TextBoxHelper.HasText" Value="False">
                            <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                        </Trigger>

                        <!-- Hover State -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource Brush.Input.Background.Hover}"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource Brush.Input.Border.Hover}"/>
                        </Trigger>

                        <!-- Focus State -->
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource Brush.Input.Border.Focus}"/>
                        </Trigger>

                        <!-- Disabled State -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource Brush.Input.Background.Disabled}"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource Brush.Border.Disabled}"/>
                            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Disabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
