<UserControl x:Class="EquipmentDesigner.Views.HardwareDefineWorkflow.DeviceDefine.DeviceDefineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             lex:LocalizeDictionary.DesignCulture="ko"
             lex:ResxLocalizationProvider.DefaultAssembly="EquipmentDesigner"
             lex:ResxLocalizationProvider.DefaultDictionary="Resources.Strings"
             mc:Ignorable="d"
             d:DesignHeight="1200" d:DesignWidth="800">

    <Grid>
        <!-- Main Content -->
        <StackPanel>
            <!-- Header -->
            <StackPanel Margin="0,0,0,24">
                <TextBlock Text="{lex:Loc Key=Device_DefineDevice}"
                       Style="{StaticResource Typography.H3}"
                       Foreground="{StaticResource Brush.Text.Primary}"/>
                <TextBlock Text="{lex:Loc Key=Device_Description}"
                       Style="{StaticResource Typography.Body}"
                       Foreground="{StaticResource Brush.Text.Secondary}"
                       Margin="0,8,0,0"/>
            </StackPanel>

            <!-- Action Buttons -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                <Button Command="{Binding LoadFromServerCommand}"
                    Padding="{StaticResource Spacing.Padding.Button}"
                    Height="{StaticResource Size.Button.Height.MD}"
                    Background="{StaticResource Brush.Button.Secondary.Background}"
                    BorderThickness="0"
                    Cursor="Hand"
                    Margin="0,0,12,0"
                    IsEnabled="{Binding IsEditable}">
                <StackPanel Orientation="Horizontal">
                    <Path Data="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"
                          Stroke="{StaticResource Brush.Text.Primary}"
                          StrokeThickness="2"
                          Width="16" Height="16"
                          Stretch="Uniform"
                          VerticalAlignment="Center"/>
                    <TextBlock Text="{lex:Loc Key=Equipment_LoadFromServer}"
                               Style="{StaticResource Typography.Button}"
                               Foreground="{StaticResource Brush.Text.Primary}"
                               Margin="8,0,0,0"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
            <Button Command="{Binding CompleteWorkflowCommand}"
                    Padding="{StaticResource Spacing.Padding.Button}"
                    Height="{StaticResource Size.Button.Height.MD}"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="{StaticResource Radius.LG}"/>
                    </Style>
                </Button.Resources>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="{StaticResource Brush.Button.Success.Background}"/>
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="{StaticResource Brush.Button.Disabled.Background}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <StackPanel Orientation="Horizontal">
                    <Path Data="M5 13l4 4L19 7"
                          StrokeThickness="2"
                          Width="16" Height="16"
                          Stretch="Uniform"
                          VerticalAlignment="Center">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Setter Property="Stroke" Value="{StaticResource Brush.Text.Inverse}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsEnabled, RelativeSource={RelativeSource AncestorType=Button}}" Value="False">
                                        <Setter Property="Stroke" Value="{StaticResource Brush.Button.Disabled.Foreground}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                    <TextBlock Text="{lex:Loc Key=Device_CompleteWorkflow}"
                               Margin="8,0,0,0"
                               VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource Typography.Button}">
                                <Setter Property="Foreground" Value="{StaticResource Brush.Text.Inverse}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsEnabled, RelativeSource={RelativeSource AncestorType=Button}}" Value="False">
                                        <Setter Property="Foreground" Value="{StaticResource Brush.Button.Disabled.Foreground}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Form Card -->
        <Border Background="{StaticResource Brush.Surface.Primary}"
                BorderBrush="{StaticResource Brush.Border.Primary}"
                BorderThickness="{StaticResource Border.Thin}"
                CornerRadius="{StaticResource Radius.LG}"
                Padding="{StaticResource Spacing.Padding.Card}"
                Margin="0,0,0,24">
            <StackPanel>

                <!-- Parent Unit ID (Read-only/Display) -->
                <StackPanel Margin="0,0,0,16"
                            Visibility="{Binding ParentUnitId, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="{lex:Loc Key=Device_ParentUnit}"
                               Style="{StaticResource Typography.Label}"
                               Foreground="{StaticResource Brush.Text.Primary}"
                               Margin="0,0,0,6"/>
                    <Border Background="{StaticResource Brush.Background.Tertiary}"
                            CornerRadius="{StaticResource Radius.MD}"
                            Padding="12,8">
                        <TextBlock Text="{Binding ParentUnitId}"
                                   Style="{StaticResource Typography.Body}"
                                   Foreground="{StaticResource Brush.Text.Secondary}"/>
                    </Border>
                </StackPanel>

                <!-- Name (Required) -->
                <StackPanel Margin="0,0,0,16">
                    <TextBlock Style="{StaticResource Typography.Label}"
                               Foreground="{StaticResource Brush.Text.Primary}"
                               Margin="0,0,0,6">
                        <Run Text="{lex:Loc Key=Equipment_Name}"/>
                        <Run Text="*" Foreground="{StaticResource Brush.Status.Danger}"/>
                    </TextBlock>
                    <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Height="{StaticResource Size.Input.Height.MD}"
                             Padding="{StaticResource Spacing.Padding.Input}"
                             Background="{StaticResource Brush.Input.Background}"
                             BorderBrush="{StaticResource Brush.Input.Border}"
                             VerticalContentAlignment="Center"
                             IsReadOnly="{Binding IsEditable, Converter={StaticResource InverseBoolConverter}}"
                             IsHitTestVisible="{Binding IsEditable}"/>
                </StackPanel>

                <!-- Display Name -->
                <StackPanel Margin="0,0,0,16">
                    <TextBlock Text="{lex:Loc Key=Equipment_DisplayName}"
                               Style="{StaticResource Typography.Label}"
                               Foreground="{StaticResource Brush.Text.Primary}"
                               Margin="0,0,0,6"/>
                    <TextBox Text="{Binding DisplayName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Height="{StaticResource Size.Input.Height.MD}"
                             Padding="{StaticResource Spacing.Padding.Input}"
                             Background="{StaticResource Brush.Input.Background}"
                             BorderBrush="{StaticResource Brush.Input.Border}"
                             VerticalContentAlignment="Center"
                             IsReadOnly="{Binding IsEditable, Converter={StaticResource InverseBoolConverter}}"
                             IsHitTestVisible="{Binding IsEditable}"/>
                </StackPanel>

                <!-- Subname -->
                <StackPanel Margin="0,0,0,16">
                    <TextBlock Text="{lex:Loc Key=Equipment_Subname}"
                               Style="{StaticResource Typography.Label}"
                               Foreground="{StaticResource Brush.Text.Primary}"
                               Margin="0,0,0,6"/>
                    <TextBox Text="{Binding Subname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Height="{StaticResource Size.Input.Height.MD}"
                             Padding="{StaticResource Spacing.Padding.Input}"
                             Background="{StaticResource Brush.Input.Background}"
                             BorderBrush="{StaticResource Brush.Input.Border}"
                             VerticalContentAlignment="Center"
                             IsReadOnly="{Binding IsEditable, Converter={StaticResource InverseBoolConverter}}"
                             IsHitTestVisible="{Binding IsEditable}"/>
                </StackPanel>

                <!-- Description -->
                <StackPanel Margin="0,0,0,16">
                    <TextBlock Text="{lex:Loc Key=Equipment_DescriptionLabel}"
                               Style="{StaticResource Typography.Label}"
                               Foreground="{StaticResource Brush.Text.Primary}"
                               Margin="0,0,0,6"/>
                    <TextBox Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Height="80"
                             Padding="{StaticResource Spacing.Padding.Input}"
                             Background="{StaticResource Brush.Input.Background}"
                             BorderBrush="{StaticResource Brush.Input.Border}"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             IsReadOnly="{Binding IsEditable, Converter={StaticResource InverseBoolConverter}}"
                             IsHitTestVisible="{Binding IsEditable}"/>
                </StackPanel>

                <!-- Implementation Guidelines -->
                <StackPanel>
                    <TextBlock Text="{lex:Loc Key=System_ImplementationGuidelines}"
                               Style="{StaticResource Typography.Label}"
                               Foreground="{StaticResource Brush.Text.Primary}"
                               Margin="0,0,0,6"/>
                    <TextBox Text="{Binding ImplementationGuidelines, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Height="100"
                             Padding="{StaticResource Spacing.Padding.Input}"
                             Background="{StaticResource Brush.Input.Background}"
                             BorderBrush="{StaticResource Brush.Input.Border}"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             IsReadOnly="{Binding IsEditable, Converter={StaticResource InverseBoolConverter}}"
                             IsHitTestVisible="{Binding IsEditable}"/>
                </StackPanel>

            </StackPanel>
        </Border>

        <!-- Commands Section -->
        <Border Background="{StaticResource Brush.Surface.Primary}"
                BorderBrush="{StaticResource Brush.Border.Primary}"
                BorderThickness="{StaticResource Border.Thin}"
                CornerRadius="{StaticResource Radius.LG}"
                Padding="{StaticResource Spacing.Padding.Card}"
                Margin="0,0,0,24">
            <StackPanel>
                <!-- Commands Header -->
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{lex:Loc Key=System_Commands}"
                                   Style="{StaticResource Typography.H4}"
                                   Foreground="{StaticResource Brush.Text.Primary}"/>
                        <TextBlock Text="{lex:Loc Key=Device_CommandsDescription}"
                                   Style="{StaticResource Typography.Body.Small}"
                                   Foreground="{StaticResource Brush.Text.Secondary}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                    <Button Grid.Column="1"
                            Command="{Binding AddCommandCommand}"
                            Padding="{StaticResource Spacing.Padding.Button}"
                            Height="{StaticResource Size.Button.Height.SM}"
                            Background="{StaticResource Brush.Button.Primary.Background}"
                            BorderThickness="0"
                            Cursor="Hand"
                            IsEnabled="{Binding IsEditable}">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="M12 4v16m8-8H4"
                                  Stroke="{StaticResource Brush.Text.Inverse}"
                                  StrokeThickness="2"
                                  Width="12" Height="12"
                                  Stretch="Uniform"
                                  VerticalAlignment="Center"/>
                            <TextBlock Text="{lex:Loc Key=System_AddCommand}"
                                       Style="{StaticResource Typography.Body.Small}"
                                       Foreground="{StaticResource Brush.Text.Inverse}"
                                       Margin="6,0,0,0"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <!-- Commands List (Card Format) -->
                <ItemsControl ItemsSource="{Binding Commands}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource Brush.Background.Tertiary}"
                                    CornerRadius="{StaticResource Radius.LG}"
                                    Padding="16,12"
                                    Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Command Info -->
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <!-- Command Name (Bold) -->
                                        <TextBlock Text="{Binding Name}"
                                                   Style="{StaticResource Typography.Body}"
                                                   FontWeight="Bold"
                                                   Foreground="{StaticResource Brush.Text.Primary}"/>
                                        <!-- Command Description -->
                                        <TextBlock Text="{Binding Description}"
                                                   Style="{StaticResource Typography.Body.Small}"
                                                   Foreground="{StaticResource Brush.Text.Secondary}"
                                                   TextTrimming="CharacterEllipsis"
                                                   Margin="0,2,0,0"/>
                                        <!-- Parameters Display (type name, type name format) -->
                                        <TextBlock Text="{Binding ParametersDisplayText}"
                                                   Style="{StaticResource Typography.Body.Small}"
                                                   Foreground="{StaticResource Brush.Text.Secondary}"
                                                   Visibility="{Binding ParametersDisplayText, Converter={StaticResource StringToVisibilityConverter}}"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <!-- Remove Button -->
                                    <Button Grid.Column="1"
                                            Command="{Binding DataContext.RemoveCommandCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            Padding="8"
                                            VerticalAlignment="Center"
                                            IsEnabled="{Binding DataContext.IsEditable, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                        <Path Data="M6 18L18 6M6 6l12 12"
                                              Stroke="{StaticResource Brush.Text.Tertiary}"
                                              StrokeThickness="2"
                                              Width="14" Height="14"
                                              Stretch="Uniform"/>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Empty State for Commands -->
                <Border Background="{StaticResource Brush.Background.Tertiary}"
                        CornerRadius="{StaticResource Radius.LG}"
                        Padding="24"
                        Visibility="{Binding HasNoCommands, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Visible}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="{lex:Loc Key=System_NoCommands}"
                                   Style="{StaticResource Typography.Body}"
                                   Foreground="{StaticResource Brush.Text.Secondary}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="{lex:Loc Key=Device_NoCommandsHint}"
                                   Style="{StaticResource Typography.Body.Small}"
                                   Foreground="{StaticResource Brush.Text.Tertiary}"
                                   HorizontalAlignment="Center"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- IO Configurations Section -->
        <Border Background="{StaticResource Brush.Surface.Primary}"
                BorderBrush="{StaticResource Brush.Border.Primary}"
                BorderThickness="{StaticResource Border.Thin}"
                CornerRadius="{StaticResource Radius.LG}"
                Padding="{StaticResource Spacing.Padding.Card}">
            <StackPanel>
                <!-- IO Header -->
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{lex:Loc Key=Device_IoConfigurations}"
                                   Style="{StaticResource Typography.H4}"
                                   Foreground="{StaticResource Brush.Text.Primary}"/>
                        <TextBlock Text="{lex:Loc Key=Device_IoDescription}"
                                   Style="{StaticResource Typography.Body.Small}"
                                   Foreground="{StaticResource Brush.Text.Secondary}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                    <Button Grid.Column="1"
                            Command="{Binding AddIoCommand}"
                            Padding="{StaticResource Spacing.Padding.Button}"
                            Height="{StaticResource Size.Button.Height.SM}"
                            Background="{StaticResource Brush.Button.Primary.Background}"
                            BorderThickness="0"
                            Cursor="Hand"
                            IsEnabled="{Binding IsEditable}">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="M12 4v16m8-8H4"
                                  Stroke="{StaticResource Brush.Text.Inverse}"
                                  StrokeThickness="2"
                                  Width="12" Height="12"
                                  Stretch="Uniform"
                                  VerticalAlignment="Center"/>
                            <TextBlock Text="{lex:Loc Key=Device_AddIo}"
                                       Style="{StaticResource Typography.Body.Small}"
                                       Foreground="{StaticResource Brush.Text.Inverse}"
                                       Margin="6,0,0,0"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <!-- IO List -->
                <ItemsControl ItemsSource="{Binding IoConfigurations}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource Brush.Background.Tertiary}"
                                    BorderBrush="{StaticResource Brush.Border.Primary}"
                                    BorderThickness="0.8"
                                    CornerRadius="{StaticResource Radius.MD}"
                                    Padding="12,8"
                                    Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- IO Name and Type (Left) -->
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}"
                                                   Style="{StaticResource Typography.Body}"
                                                   FontWeight="Bold"
                                                   Foreground="{StaticResource Brush.Text.Primary}"/>
                                        <TextBlock Text="{Binding IoType}"
                                                   Style="{StaticResource Typography.Body.Small}"
                                                   Foreground="{StaticResource Brush.Text.Secondary}"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <!-- Address Badge (Right) -->
                                    <Border Grid.Column="1"
                                            Background="{StaticResource Brush.Background.Secondary}"
                                            CornerRadius="{StaticResource Radius.SM}"
                                            Padding="8,4"
                                            VerticalAlignment="Center"
                                            Margin="12,0">
                                        <TextBlock Text="{Binding Address}"
                                                   FontFamily="Consolas"
                                                   FontSize="14"
                                                   Foreground="{StaticResource Brush.Text.Secondary}"/>
                                    </Border>

                                    <!-- Remove Button -->
                                    <Button Grid.Column="2"
                                            Command="{Binding DataContext.RemoveIoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            Padding="4"
                                            VerticalAlignment="Center"
                                            IsEnabled="{Binding DataContext.IsEditable, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                        <Path Data="M6 18L18 6M6 6l12 12"
                                              Stroke="{StaticResource Brush.Text.Tertiary}"
                                              StrokeThickness="2"
                                              Width="14" Height="14"
                                              Stretch="Uniform"/>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Empty State for IO -->
                <Border Background="{StaticResource Brush.Background.Tertiary}"
                        CornerRadius="{StaticResource Radius.LG}"
                        Padding="24"
                        Visibility="{Binding HasNoIoConfigurations, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Visible}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="{lex:Loc Key=Device_NoIo}"
                                   Style="{StaticResource Typography.Body}"
                                   Foreground="{StaticResource Brush.Text.Secondary}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="{lex:Loc Key=Device_NoIoHint}"
                                   Style="{StaticResource Typography.Body.Small}"
                                   Foreground="{StaticResource Brush.Text.Tertiary}"
                                   HorizontalAlignment="Center"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </StackPanel>
</Grid>
</UserControl>