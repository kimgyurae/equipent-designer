<Window x:Class="EquipmentDesigner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EquipmentDesigner"
        xmlns:dashboard="clr-namespace:EquipmentDesigner.Views.Dashboard"
        xmlns:workflow="clr-namespace:EquipmentDesigner.Views.HardwareDefineWorkflow"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture="ko"
        lex:ResxLocalizationProvider.DefaultAssembly="EquipmentDesigner"
        lex:ResxLocalizationProvider.DefaultDictionary="Resources.Strings"
        mc:Ignorable="d"
        Title="{lex:Loc Key=AppTitle}" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <!-- Content will be set dynamically via code-behind navigation -->
        <ContentControl x:Name="MainContent"/>
        
        <!-- Full-screen Backdrop Overlay (shown when dialogs are open) -->
        <Border x:Name="BackdropOverlay"
                Background="{StaticResource Brush.Overlay.Backdrop}"
                Visibility="Collapsed"/>
    </Grid>
</Window>